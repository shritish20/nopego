/**
 * SplashScreen — auto-show on page load DISABLED per handoff rule:
 * "No popup on page load. Popups must only open if user clicks."
 * Component returns null. Kept so imports don't break.
 */
export default function SplashScreen() {
  return null
}
